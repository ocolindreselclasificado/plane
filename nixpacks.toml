[phases.setup]
nixPkgs = ['nodejs', 'yarn']

[phases.build]
cmds = [
  'yarn install --immutable --immutable-cache',
  'yarn turbo run build --filter=web',
  'bundle exec rails db:migrate'  # 👈 Run migration during build!
]

[start]
cmd = 'yarn workspace web start'
